# Suppress npm funding and audit messages during installation
fund=false
audit=false

# Resolve sharp version conflicts
prefer-dedupe=true

# Avoid forcing sharp source builds when Homebrew libvips is present
sharp_ignore_global_libvips=true
